<Window x:Class="TaskManager.Views.TaskModView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local0="clr-namespace:TaskManager"
        xmlns:local="clr-namespace:TaskManager.Views"
        xmlns:local1="clr-namespace:TaskManager.Helpers"
        mc:Ignorable="d"
        Title="Задача" Height="380" Width="420">

    <Window.Resources>
        <local1:PriorityConverter x:Key="PrConv" />
        <local1:StatusConverter x:Key="StConv" />
    </Window.Resources>

    <Window.CommandBindings>
        <CommandBinding Command="local0:WindowCommands.TaskOk" Executed="BtnOk_Click"/>
        <CommandBinding Command="local0:WindowCommands.TaskCancel" Executed="BtnCancel_Click"/>
    </Window.CommandBindings>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="110"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Название:" VerticalAlignment="Center" Margin="0 8 0 4"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="TxtName" Margin="0 8 0 4" Height="24"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Описание:" VerticalAlignment="Top" Margin="0 8 0 4"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="TxtDescription" Margin="0 8 0 4" 
                 Height="80" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Приоритет:" VerticalAlignment="Center" Margin="0 8 0 4"/>
        <ComboBox Grid.Row="2" Grid.Column="1" x:Name="PriorityField" Margin="0 8 0 4" Height="24">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Converter={StaticResource PrConv}}" />
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Срок:" VerticalAlignment="Center" Margin="0 8 0 4"/>
        <DatePicker Grid.Row="3" Grid.Column="1" x:Name="DpDate" Margin="0 8 0 4" Height="24"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="Статус:" VerticalAlignment="Center" Margin="0 8 0 4"/>
        <ComboBox Grid.Row="4" Grid.Column="1" x:Name="StatusField" Margin="0 8 0 4" Height="24">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Converter={StaticResource StConv}}" />
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <CheckBox Grid.Row="5" Grid.Column="1" x:Name="ChkImportant" 
                  Content="Важная задача" Margin="0 8 0 4"/>

        <StackPanel Grid.Row="7" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 16 0 0">
            <Button Style="{StaticResource MainButtonStyle}" Command="local0:WindowCommands.TaskOk" Content="ОК" Width="90" Height="30" Margin="0 0 8 0"/>
            <Button Style="{StaticResource MainButtonStyle}" Command="local0:WindowCommands.TaskCancel" Content="Отмена" Width="90" Height="30"/>
        </StackPanel>
    </Grid>
</Window>